# Local dev secrets — gitignored, never commit. Fill in the two values below with your
# Supabase project's details (Project Settings → API in the Supabase dashboard).
SUPABASE_URL=https://jpgyrvdpcemhmjmklyrq.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb_secret_d8VNUhbj_jF6UZKOMgl9dw_o-0sh-Zd

# Automatic product photo lookup (see .env.example for how to get these) — leave blank to
# disable, products will just use the placeholder image until one is uploaded manually.
GOOGLE_CSE_API_KEY=
GOOGLE_CSE_ID=
